module src::commands::scan_results::set_property::scan_results_set_property_response;

// Port of commands/scan_results/set_property/scan_results_set_property_response.rs:
// upstream is an empty struct — property writes are best-effort and failures
// only warn.
pub data ScanResultsSetPropertyResponse {}

// Upstream `Default`.
pub machine ScanResultsSetPropertyResponse::empty() -> ScanResultsSetPropertyResponse {
    ScanResultsSetPropertyResponse {}
}
