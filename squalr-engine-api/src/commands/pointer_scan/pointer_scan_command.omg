module src::commands::pointer_scan::pointer_scan_command;

use src::commands::pointer_scan::expand::pointer_scan_expand_request::PointerScanExpandRequest;
use src::commands::pointer_scan::reset::pointer_scan_reset_request::PointerScanResetRequest;
use src::commands::pointer_scan::start::pointer_scan_start_request::PointerScanStartRequest;
use src::commands::pointer_scan::summary::pointer_scan_summary_request::PointerScanSummaryRequest;
use src::commands::pointer_scan::validate::pointer_scan_validate_request::PointerScanValidateRequest;

// Port of commands/pointer_scan/pointer_scan_command.rs — the pointer-scan
// command union, in upstream case order (Reset, Start, Summary, Expand,
// Validate).
pub data PointerScanCommand {
    case Reset(request: PointerScanResetRequest);
    case Start(request: PointerScanStartRequest);
    case Summary(request: PointerScanSummaryRequest);
    case Expand(request: PointerScanExpandRequest);
    case Validate(request: PointerScanValidateRequest);
}
