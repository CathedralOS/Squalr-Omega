module src::commands::pointer_scan::start::pointer_scan_start_request;

use src::structures::data_values::pointer_scan_pointer_size::PointerScanPointerSize;
use src::structures::pointer_scans::pointer_scan_address_space::PointerScanAddressSpace;
use src::structures::pointer_scans::pointer_scan_target_request::PointerScanTargetRequest;

// Port of commands/pointer_scan/start/pointer_scan_start_request.rs.
pub data PointerScanStartRequest {
    target: PointerScanTargetRequest;
    pointer_size: PointerScanPointerSize;
    max_depth: u64;
    offset_radius: u64;
    address_space: PointerScanAddressSpace;
}

pub machine PointerScanStartRequest::new(
    target: PointerScanTargetRequest,
    pointer_size: PointerScanPointerSize,
    max_depth: u64,
    offset_radius: u64,
    address_space: PointerScanAddressSpace
) -> PointerScanStartRequest {
    PointerScanStartRequest {
        target: target,
        pointer_size: pointer_size,
        max_depth: max_depth,
        offset_radius: offset_radius,
        address_space: address_space,
    }
}
