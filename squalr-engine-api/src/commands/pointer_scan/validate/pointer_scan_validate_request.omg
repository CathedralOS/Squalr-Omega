module src::commands::pointer_scan::validate::pointer_scan_validate_request;

use src::structures::pointer_scans::pointer_scan_target_request::PointerScanTargetRequest;

// Port of commands/pointer_scan/validate/pointer_scan_validate_request.rs.
pub data PointerScanValidateRequest {
    session_id: u64;
    target: PointerScanTargetRequest;
}

pub machine PointerScanValidateRequest::new(
    session_id: u64,
    target: PointerScanTargetRequest
) -> PointerScanValidateRequest {
    PointerScanValidateRequest {
        session_id: session_id,
        target: target,
    }
}
