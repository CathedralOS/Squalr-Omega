module src::structures::memory::endian;

// Port of Endian. Upstream derives Ord/Serialize/FromStr; the enum variants
// and equality are what ported callers consume.
pub data Endian [copy] {
    case Little;
    case Big;
}
