module src::structures::scanning::plans::plan_types::planned_scan_type_scalar;

// Port of PlannedScanTypeScalar: the scalar scan implementations a plan may
// select.
pub data PlannedScanTypeScalar [copy] {
    case SingleElement;
    case ScalarIterative;
}
