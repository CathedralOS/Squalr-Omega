module src::structures::scanning::plans::plan_types::planned_scan_vectorization_size;

// Port of PlannedScanVectorizationSize: the hardware vector widths a vector
// plan may target (128/256/512-bit lanes).
pub data PlannedScanVectorizationSize [copy] {
    case Vector16;
    case Vector32;
    case Vector64;
}
