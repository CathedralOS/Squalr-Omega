module src::pointer_scans::search_kernels::mod;

// Mirrors search_kernels/mod.rs. Omega `use` wiring in the package main
// pulls the compiled set; this file exists only to keep the upstream layout.
